'use strict';

angular.module('mean.app.dashboard')
    .controller('DashboardCtrl', function ($scope, $state, $location, projects) {
    	// $scope.projects = projects;
    	// $scope.cards = cards;

        console.log("dashboard controller");
        console.log(projects);

});